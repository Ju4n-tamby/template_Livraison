-----------------------------------
          DATABASE
-----------------------------------
  - creer table :   (Juan)
      * Liste livreur : id, nom, salaire
      * Liste vehicule : id, marque
      * Liste zone_de_livraison : id, zone
      * Statut(en attente livre annulee)
      * Param_livraison : prix_kg, adresse depart
      * Colis : id, libelle, poids
      * Liste livraison : id, vehicule, chauffeur, montant recette(cout revient vehicule),  zone_de_livraison, adresse de destination, colis, date

  - inserer des valeurs de test (Juan)

-----------------------------------
          FONCTIONS
-----------------------------------
  - creer une fonction connect(connection base) (juan)

  - function getAllStatut() (Mihaja)

  - function getAllChauffeur() (Mihaja)

  - function getAllVehicule() (Mihaja)

  - function gatAllZone() (Mihaja)

  - function getParametre() (Mihaja)

  - function insererColis($libelle, $poids) (juan)

  - function insererLivraison($idcolis, $vehicule, $chauffeur, $date, $statut, $montant recette) (juan)

  - function getAlllivraison($jour, $mois, $annee)

  - function calculChiffreAffaire($idColis) (juan)

  - function getAllRevient($livraisons)

  - function getBenefice($jour, $mois, $annee)

-----------------------------------
          MODELS (Mihaja)
-----------------------------------
  - Vehicule.php : (Mihaja)
    * function : getAllVehicule()

  - livreur.php  (Mihaja)
    * function getAllChauffeur()

  - statut.php (Mihaja)
    * function getAllStatut()

  - zone.php (Mihaja)
    * getAllZone()

  - colis.php
    * inserer colis

  - livraison.php
    * function inserer livraison(), function getParametre()

  - benefice.php
    * function calculChiffreAffaire(), getAllRevient()

-----------------------------------
          CONTROLLERS (Juan)
-----------------------------------
  - LivraisonController.php :
    * functions :
      - getDonneesFormulaires()
        -> ['chauffeurs', 'status', 'vehicules', 'zones']

      - insererDonnees()
        -> get les inputs des formulaires
        -> inserer colis et get son id pour inserer dans livraison
        -> return true if reussie et false sinon

      - getBenefice($jour, $mois, $annee)
        -> return date et benefice

-----------------------------------
          VUES (Juan)
-----------------------------------
  - creer des vues :
      - pages pour formulaire insertion livraison
        * input date
        * select chauffeur value idlivreur
        * select vehicule value idVehicule
        * select status value idStatut
        * select zone value idZone
        * input colis (libelle et pods)

      - pages pour afficher benefice par periode

-----------------------------------
          ROUTES
-----------------------------------
  - creer les routes necessaires dans routes.php pour les vues


PARTIE 2 :

  - DATABASE : (Mihaja)
    * ajouter une colonne bonus default 0 dans zone
    * inserer les donnée :
      - 10 vehicules
      - 5 chauffeur 15000AR, 3 chauffeur 18000AR, 4 chauffeur 20000Ar
      - 3 zones à 12.5%, 2 zones 0%

  - function getSalaire (Juan)
    * modifier pour ajouter les bonus

  - vues : (Juan)
    * ajouter une page pour gerer les zones(formulaire ajout, liste avec option modifierou supprimer)
    * livraisons.php:
      - ajouter un bouton pour tout supprimer avec validation taper 9999 pour confirmer
